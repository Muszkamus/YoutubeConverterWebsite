services:
  api:
    build:
      context: ../..
      dockerfile: docker/back/Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - downloads:/app/downloads
    restart: unless-stopped

volumes:
  downloads:
